#include <iostream>
#include <string>
#include <map>
using namespace std;

struct nodetype {
    char symbol;
    int frequency;
    nodetype* left;
    nodetype* right;
};

void generateHuffmanCodes(nodetype* root, string code, map<char, string>& codes) {
    
}
int main() {
    cout <<"hi\n";

}